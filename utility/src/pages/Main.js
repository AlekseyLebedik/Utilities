import React from "react";
import utility from "@utility";
import { get } from "lodash";
import { MOCK, MOCK_1, MOCK_2, DEEP_MOCK } from "@utility/mock";
import { isDeepEmpty, pathFieldObject } from "@utility/DataUtility/Object";

const Main = (props) => {
  return <div>Main page</div>;
};

export default Main;
